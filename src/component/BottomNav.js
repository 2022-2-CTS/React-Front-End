import React from "react";

const Nav = () => {
    return (
        <React.Fragment>
            <div>
                nav bar
            </div>
        </React.Fragment>
    );
};

export default Nav;